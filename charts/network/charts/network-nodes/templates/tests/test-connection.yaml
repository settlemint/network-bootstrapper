apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodes.fullname" . }}-test-connection"
  labels:
    {{- include "nodes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
