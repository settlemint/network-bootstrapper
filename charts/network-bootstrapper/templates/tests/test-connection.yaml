apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "network-bootstrapper.fullname" . }}-test-connection"
  labels:
    {{- include "network-bootstrapper.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "network-bootstrapper.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
