pre-commit:
  commands:
    check_fix:
      run: bun run check:fix || true
      stage_fixed: true
    docs_cli:
      run: |
        if bun run docs:cli; then
          git add README.md
        fi
      stage_fixed: true